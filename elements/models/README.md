Place .glb model files in this folder. Name a model `test-model.glb` to have it load by default in the viewer. The viewer uses the <model-viewer> web component and supports glTF/GLB files.

If you want to change the default file shown, edit `index.html` and update the `src` attribute of the <model-viewer> in the `#viewer` section.
